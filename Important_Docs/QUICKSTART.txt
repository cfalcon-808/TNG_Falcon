============================================================
TIGERS & GOATS - FALCON BRANCH
QUICKSTART WORKFLOW (Unified Env, Experiments & Variations)
============================================================

This guide walks new users through:
  - environment setup
  - running a training experiment (suite of variations)
  - evaluating and debugging trained models

----------------------------------------------------------------
0) Prerequisites
----------------------------------------------------------------
- Python 3.10+ recommended
- NVIDIA GPU optional but preferred
- Install PyTorch matching your CUDA version if using GPU

----------------------------------------------------------------
1) Create a virtual environment & install dependencies
----------------------------------------------------------------
    python -m venv .venv
    .\.venv\Scripts\activate          # Windows PowerShell
    pip install --upgrade pip
    pip install -r docs/requirements.txt

    # Then install the correct PyTorch wheel for your CUDA version:
    # https://pytorch.org/get-started/locally/

----------------------------------------------------------------
2) Project layout (key files)
----------------------------------------------------------------
    env_tng_falcon.py
        Unified Gymnasium environment.
        Switches between greedy or smart tiger via TIGER_AI_MODE.

    train_falcon.py
        Training runner.
        Executes ONE EXPERIMENT (suite) consisting of multiple VARIATIONS.
        Supports single-phase dicts or multi-phase lists (curricula/mix).

    eval_falcon.py
        Evaluation & debugging tool.
        Runs deterministic rollouts and batch statistics.

    tng_GUI_falcon.py
        Tkinter GUI for live play, quick model smoke tests, and replay browsing.

    artifacts/
        models/train/{ALGO}/{CORE}/{EXPERIMENT}/
        logging/train/{ALGO}/{CORE}/{EXPERIMENT}/
        logging/eval_debug/

    (legacy) env_goat_falcon.py / env_tiger_falcon.py
        Present for reference; current runs use env_tng_falcon.py.

----------------------------------------------------------------
3) Run a fast training experiment (smoke test)
----------------------------------------------------------------
1) Open train_falcon.py and set:
        EXPERIMENT_NAME = "my_first_experiment"
        TIGER_AI_MODE   = TIGER_AI_GREEDY   # or TIGER_AI_SMART
        DEBUG_MODE      = True              # shorter runs

2) Define variations (single-phase example):
        VARIATIONS = {
            "defaultSettings": None
        }

   Multi-phase example (mixing greedy and smart tigers):
        VARIATIONS = {
            "greedy_to_smart_mix": [
                {"timesteps": 10_000_000, "tiger_ai": TIGER_AI_GREEDY, "reward_weights": None},
                {"timesteps": 10_000_000, "tiger_ai": TIGER_AI_SMART,  "reward_weights": None},
                {"timesteps": 30_000_000, "tiger_ai": TIGER_AI_SMART,  "mix_prob": 0.3, "reward_weights": None},
                {"timesteps": 30_000_000, "tiger_ai": TIGER_AI_SMART,  "mix_prob": 0.5, "reward_weights": None},
            ],
        }

3) Run:
        python train_falcon.py

4) Outputs:
        Models:
          artifacts/models/train/{ALGO}/{CORE}/{EXPERIMENT}/
        Logs:
          artifacts/logging/train/{ALGO}/{CORE}/{EXPERIMENT}/

----------------------------------------------------------------
4) Monitor training with TensorBoard
----------------------------------------------------------------
    tensorboard --logdir artifacts/logging/train --port 6006

Each VARIATION appears as a separate run:
    {ALGO}_{CORE}_{variation}[_p{phase}]

----------------------------------------------------------------
5) Evaluate or debug a trained model
----------------------------------------------------------------
Recommended (explicit model path):
    python eval_falcon.py battle artifacts/models/train/.../mppo_GvST_defaultSettings_p0.zip

Defaults:
    MODE (optional): normal  -> greedy tiger (default)
                     battle  -> smart tiger
    MODEL_PATH (optional): path to a trained .zip model.
      If omitted, eval_falcon.py searches its default directory
      (legacy: artifacts/models/experiment). Pass explicit paths
      for models saved under artifacts/models/train.

Evaluation modes include:
  - step-by-step debug episodes
  - batch evaluation (win / timeout rates)
  - action masks + decoded moves
  - optional ASCII board rendering

Logs are written under:
    artifacts/logging/eval_debug/

----------------------------------------------------------------
6) What to look at
----------------------------------------------------------------
Training (TensorBoard):
  - custom/episodes_total
  - goat / tiger win rates
  - timeout and stall rates
  - learning curves

Evaluation (eval logs):
  - per-step transitions
  - illegal move masking
  - winners and termination causes
  - summary statistics

----------------------------------------------------------------
7) Common configuration knobs
----------------------------------------------------------------
train_falcon.py:
    TIGER_AI_MODE       = TIGER_AI_GREEDY | TIGER_AI_SMART
    DEBUG_MODE          = True / False
    TIMESTEPS           = adjust for runtime
    VARIATIONS          = reward/knob ablations or multi-phase curricula
    DEVICE_MODE         = "gpu" or "cpu"
    NUM_CPU             = parallel env workers
    CHECKPOINTS_PER_RUN = number of checkpoints per phase

eval_falcon.py:
    N_DEBUG_EPISODES
    N_EVAL_GAMES
    PRINT_DEBUG_TO_CONSOLE

----------------------------------------------------------------
8) If something breaks
----------------------------------------------------------------
- Confirm the virtualenv is active
- Verify PyTorch matches your CUDA version
- Delete old artifacts/logging/train if TensorBoard is cluttered
- Run eval_falcon.py with an explicit model path to confirm env loads cleanly

----------------------------------------------------------------
Summary
----------------------------------------------------------------
- One EXPERIMENT = a suite of VARIATIONS
- One VARIATION  = one PPO training run (phase-aware if you use lists)
- One CORE       = one learner/opponent matchup
- Same environment file for all runs

Train with train_falcon.py,
inspect with TensorBoard,
and debug/evaluate with eval_falcon.py (or the GUI for quick visuals).
